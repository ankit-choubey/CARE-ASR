[build-system]
requires = ["setuptools>=68.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "care-asr-ner-validation"
version = "2.0.0"
description = "BioBERT NER, Medical Entity Schema, Retrieval Candidate Validation, Thresholding & Error Analysis for CARE-ASR"
authors = [
    { name = "Lead Architect", email = "architect@care-asr.org" }
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.5.0",
    "pydantic-settings>=2.1.0",
    "pyyaml>=6.0.1",
    "torch>=2.1.0",
    "transformers>=4.35.0"
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.3",
    "pytest-cov>=4.1.0",
    "black>=23.11.0",
    "ruff>=0.1.5",
    "mypy>=1.7.0"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["care_asr*"]

[tool.black]
line-length = 100
target-version = ['py311']
include = '\.pyi?$'

[tool.ruff]
line-length = 100
target-version = "py311"
select = ["E", "F", "W", "I", "N", "UP", "B", "A", "C4", "SIM"]
ignore = []

[tool.mypy]
python_version = "3.11"
strict = true
ignore_missing_imports = true
warn_unused_configs = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["care_asr/tests"]
python_files = ["test_*.py"]
addopts = "-v"
